<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"> </script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<div class="container">
<%= form_with(model: prestamo, local: true) do |form| %>
  <% if prestamo.errors.any? %>
  <div id="error_explanation">
  <h2><%= pluralize(prestamo.errors.count, "error") %> prohibited this prestamo from being saved:</h2>
  
  <ul>
  <% prestamo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
        </ul>
        </div>
        <% end %>
        
        <div class="field">
        <%= form.label :fecha %>
        <%= form.date_select :fecha %>
        </div>
        
        <div class="field">
        <%= form.label :estado %>
        <%= form.select :estado, [["pendiente", "PENDIENTE"], ["entregado","ENTREGADO"]] %>
        </div>
        
        <div class="field">
        <%= form.label :usuario_id %>
        <%= form.select :usuario_id, options_for_select(Usuario.all.map{|s|[s.nombres, s.id]}) %>
        </div>
        
        <div class="actions">
        <%= form.submit %>
        </div>
      <% end %>
</div>